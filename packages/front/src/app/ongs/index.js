import './src';

export function bootstrap() {
  return Promise
    .resolve()
    .then(() => {
    });
}

export function mount() {
  return Promise
    .resolve()
    .then(() => {
    });
}

export function unmount() {
  return Promise
    .resolve()
    .then(() => {
    });
}